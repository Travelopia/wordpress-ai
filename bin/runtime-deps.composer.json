{
  "name": "travelopia/wordpress-ai-runtime-deps",
  "description": "Manifest of the AI client runtime dependencies that get scoped into the committed `dependencies/` bundle. Installed into a throwaway build directory by bin/build-dependencies.sh — never installed into the plugin's own vendor/, so the unscoped WordPress\\AiClient\\* copy never reaches a consuming site (or this plugin's own test runner on WordPress 7.0+).",
  "require": {
    "php": "^8.3",
    "wordpress/php-ai-client": "0.4.3",
    "aysnc/wordpress-php-ai-client-bedrock": "^0.1.0",
    "php-http/guzzle7-adapter": "^1.1.0",
    "guzzlehttp/guzzle": "^7.10.0",
    "http-interop/http-factory-guzzle": "^1.2.1"
  },
  "config": {
    "allow-plugins": {
      "php-http/discovery": true
    }
  }
}
